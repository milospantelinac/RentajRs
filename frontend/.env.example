# Root environment file consumed by docker-compose.yml.
# Copy to .env and adjust if you need non-default ports/credentials.
# Service-specific secrets live in backend/.env and frontend/.env (copy from their .env.example files).

POSTGRES_USER=rentaj
POSTGRES_PASSWORD=rentaj_dev_password
POSTGRES_DB=rentaj
POSTGRES_PORT=5432

REDIS_PORT=6379

MAILDEV_SMTP_PORT=1025
MAILDEV_WEB_PORT=1080

ADMINER_PORT=8081

BACKEND_PORT=3001
FRONTEND_PORT=3000

NUXT_PUBLIC_API_BASE=http://localhost:3001/api/v1
